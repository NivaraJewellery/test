# Shop by Category text placement fix (v9)

- Resets legacy `.category span` absolute-positioning styles inherited from the older category design.
- Keeps the category image in the upper image area.
- Places the category name and `Explore Now →` in the dedicated lower section of each card.
- Preserves `object-fit: contain` so category images are not cropped.
